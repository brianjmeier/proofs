#set page(paper: "a4", margin: 2.5cm)
#set text(font: "New Computer Modern", size: 11pt)
#set par(justify: true)

#align(center, text(14pt, weight: "bold")[
  Cyclic Subgroups and Halving
])

= Introduction

Let $F$ be a finite field. Within its non-zero elements $F^times$, we choose an element $omega$ that is a primitive root of order $2^n$ with $n >= 1$, i.e. $omega^(2^n) = 1$ and no smaller positive power of $omega$ gives $1$.

*Note:* We require $n >= 1$ so that the initial order $|D_0| = 2^n$ is even. This is the hypothesis we need for the halving to begin.

From $omega$, we construct a chain of subsets $D_0, D_1, D_2, dots$ where each is obtained by squaring the previous one, $D_k = D_(k-1)^2 = {x^2 : x in D_(k-1)}$:

$D_0 := {1, omega, omega^2, omega^3, dots, omega^(2^n - 1)}$

$D_1 := {1, omega^2, omega^4, omega^6, dots, omega^(2(2^(n-1) - 1))}$

In general:

$ D_k := {1, omega^(2^k), omega^(2 dot 2^k), omega^(3 dot 2^k), dots}. $

*Def:* A *cyclic group* is one that can be generated by a single element. If we take an element $g$ and look at all its powers ${1, g, g^2, g^3, dots}$, we eventually return to $1$ (because we are in a finite group). The number of distinct elements before repeating is called the *order* of the group.

Let's see that $D_k$ is cyclic with generator $omega^(2^k)$. By definition, every element of $D_k$ is a power of $omega^(2^k)$. Since $(omega^(2^k))^(2^(n-k)) = omega^(2^n) = 1$, the powers repeat after $2^(n-k)$ steps, and no earlier (because $omega$ is primitive). Thus $D_k$ is exactly the cyclic group generated by $omega^(2^k)$.

Now the question is, for a given $k$, what's the order of $D_k$?

=== Let's start with a concrete example

Let's say $n = 3$, that is, $omega$ has order $2^3 = 8$.

- $D_0 = {1, omega, omega^2, omega^3, omega^4, omega^5, omega^6, omega^7}$.

- $D_1$ comes from squaring each element of $D_0$:

  $D_1 &= {1^2, omega^2, (omega^2)^2, (omega^3)^2, (omega^4)^2, (omega^5)^2, (omega^6)^2, (omega^7)^2}\
  &={1, omega^2, omega^4, omega^6, omega^8, omega^10, omega^12, omega^14}\
  &={1, omega^2, omega^4, omega^6, omega^0, omega^2, omega^4, omega^6}\
  &={1, omega^2, omega^4, omega^6}$

  So, $|D_0| = 8$ and $|D_1|=4$. Let's continue.

- $D_2 = {(omega^2)^2, (omega^4)^2, (omega^6)^2, 1^2} = {1, omega^4, omega^8, omega^12} = {1, omega^4, 1, omega^4} = {1, omega^4}$.

  It has $2$ elements.

- $D_3 = {1^2, (omega^4)^2} = {1, omega^8} = {1, 1} = {1}$.

  It has $1$ element.

We observe a clear pattern: $8 -> 4 -> 2 -> 1$. Each step divides by $2$.

#pagebreak()

= Hypothesis

From the example above, a natural hypothesis emerges:

For each $k < n$, the order of $D_k$ is exactly double the order of $D_(k+1)$:

$ |D_k| = 2 dot |D_(k+1)|. $

= Proof

*Theorem.* For every $k < n$, we have $|D_(k+1)| = (|D_k|) / 2$.

*Proof by induction on $k$.*

*Base case* ($k = 0$): We need to show $|D_1| = (|D_0|) / 2$. We know $|D_0| = 2^n$ because $D_0$ contains all powers of $omega$ up to $omega^(2^n - 1)$. Looking at the example above, we saw that squaring the 8 elements of $D_0$ produced only 4 distinct elements in $D_1$. This happens because some pairs of elements in $D_0$ have the same square. In fact, for every $y$ in $D_1$, there are exactly two elements $x$ in $D_0$ such that $x^2 = y$: if $x$ is one solution, then $-x$ is the other (and $x \neq -x$ since the order is even). Thus $|D_1| = |D_0| / 2 = 2^(n-1)$. $checkmark$

*Inductive step:* Assume $|D_k| = (|D_(k-1)|) / 2$ holds (equivalently, $|D_k| = 2^(n-k)$). We want to show $|D_(k+1)| = (|D_k|) / 2$.

The set $D_(k+1)$ is obtained by squaring every element of $D_k$. In a cyclic group of even order, the squaring map sends exactly two elements to each output: if $x^2 = y$, then $(-x)^2 = y$ as well, and $x \neq -x$ because the group order $2^(n-k)$ is at least 2. Therefore, the number of distinct squares is exactly half the number of original elements:

$ |D_(k+1)| = (|D_k|) / 2 = (2^(n-k)) / 2 = 2^(n-(k+1)). #h(1em) checkmark $

This completes the induction.

*Observation.* By iterating this halving, we obtain $|D_k| = 2^(n-k)$ for all $k <= n$. In particular, starting from $|D_0| = 2^n$, after $n$ halvings we reach $|D_n| = 1$. In $n$ steps, the group collapses to a single element. This exponential reduction is the fundamental algebraic property that makes this structure so useful in iterative constructions.

#h(1fr) $square$
